// Main entry point for fence editor
import { initializeFenceEditor } from './fence-events.js';

// Initialize when DOM is loaded
document.addEventListener('DOMContentLoaded', initializeFenceEditor);
